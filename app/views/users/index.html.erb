<p>This is index page that shows all users each section.</p>

<% @sections.each do |section| %>
  <p><%= section.name %></p>
  <div class="everyone">
    <ul>
      <% section.users.each do |user| %>
        <li><%= user.name %></li>
     <% end %>
   </ul>
  </div>
  <hr>
<% end %>
