<h2>みんな</h2>
<div class="everyone">
  <% @sections.each do |section| %>
      <div class="section_box">
        <p class="section_name"><%= section.name %></p>
           <ul>
            <% section.users.each do |user| %>
              <li><%= image_tag user.face_image_url(:index_thumb).to_s, :size => "60x60" %><%= link_to user.name, user %></li>
          <% end %>
         </ul>
      </div>
  <% end %>
</div>
